{% extends 'base.html.twig' %}

{% block title %}Todo Live{% endblock %}

{% block body %}
    <main class="page">
        <twig:TodoList
            page="{{ page }}"
            currentPage="{{ currentPage }}"
            totalPages="{{ totalPages }}"
        />
    </main>
{% endblock %}
